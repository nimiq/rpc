import { RpcClient as Client, PostMessageRpcClient, RedirectRpcClient, ResponseHandler } from '../dist/RpcClient';

export declare const RpcClient: {
    RpcClient: typeof Client;
    PostMessageRpcClient: typeof PostMessageRpcClient;
    RedirectRpcClient: typeof RedirectRpcClient;
    ResponseHandler: ResponseHandler;
};
