import { RpcServer as Server, CommandHandler, ResponseStatus, State } from '../dist/RpcServer';

export declare const RpcServer: {
    RpcServer: typeof Server;
    CommandHandler: CommandHandler;
    ResponseStatus: typeof ResponseStatus;
    State: typeof State;
};
